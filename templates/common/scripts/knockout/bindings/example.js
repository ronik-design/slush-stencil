/* eslint-disable */

import ko from "knockout";

const init = function (/* element, valueAccessor... */) {

  // Just here for instructional purposes.
};

export default { init, allowVirtual: true };
